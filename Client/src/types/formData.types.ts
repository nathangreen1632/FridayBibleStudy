// Client/src/types/formData.types.ts

/**
 * Contact form data structure.
 * Used by ContactFormView and related logic.
 */
export interface ContactFormData {
  name: string;
  email: string;
  message: string;
}
